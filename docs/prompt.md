 Act as a senior-backend-engineer , senior-frontend-engineer ..

Design and implement a notification email system that sends diet and workout reminders to users.

Requirements:

Use email notification templates (create ejs modern & advance level template that is fully responsive for all devices follow layout design + text fallback).

Support scheduled reminders based on user preferences (time, timezone).

Personalize emails with user data (name, diet plan, workout type).

Ensure idempotent delivery (no duplicate reminders).

Use best practices for reliability, scalability, and security.

Include proper error handling, retries, and logging.

Design the system so templates are reusable and configurable.

Deliverables:

High-level architecture (scheduler, worker, email service).

Database schema for reminders and user preferences.

Example email template (diet & workout reminder).

Notes on testing strategy (unit + integration).