export const COACH_AVATAR_VRM_URL = "/models/coach-avatar.vrm?v=default-colors-20260506";

/** Silence detection threshold (ms) - shorter for snappier response */
export const SILENCE_THRESHOLD = 1200;

/** Minimum speech length to trigger processing */
export const MIN_SPEECH_LENGTH = 2;

/** Nudge delay when coach is idle and session is active (ms) */
export const NUDGE_DELAY_MS = 25_000;
